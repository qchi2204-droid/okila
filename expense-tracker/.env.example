# Google OAuth client ID used for Gmail sync (expo-auth-session).
# Create an OAuth 2.0 client (type "Web application" works with Expo's proxy
# redirect, or "iOS"/"Android" client for native redirect schemes) in the
# Google Cloud Console and enable the Gmail API.
EXPO_PUBLIC_GOOGLE_CLIENT_ID=
